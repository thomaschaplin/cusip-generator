{
  "name": "examples",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./build && tsc",
    "watch": "rm -rf./build && tsc --watch",
    "start": "node build/generate-cusip.js"
  },
  "devDependencies": {
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "@thomaschaplin/cusip-generator": "../"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
